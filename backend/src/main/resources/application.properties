spring.application.name=senior-app
server.port=8080

# --- MySQL Configuration ---
spring.datasource.url=jdbc:mysql://localhost:${MYSQL_PORT:3333}/seniorapp?createDatabase?fNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Security & JWT ---
app.jwt.secret=${APP_JWT_SECRET:CHANGE_ME_?N_PRODUCT?ON_use_a_long_random_secret_key_at_least_256_bits}
app.jwt.expiration-ms=86400000

# --- Encryption (Jira/GitHub Token ?ifreleme i?in ?ART) ---
app.integration.crypto.key=${APP_CRYPTO_KEY:default_32_chars_long_key_required_}

# --- GitHub OAuth ---
github.client.id=${G?THUB_CL?ENT_?D:Ov23li9glpCQOHx1keV0}
github.client.secret=${G?THUB_CL?ENT_SECRET:454a9c978382b3b8569532bdb3f4b687d40c1180}
github.redirect.uri=http://localhost:5173/auth/github/callback

# --- GitHub Classical Token (PAT) ---
github.personal-access-token=${G?THUB_PAT:}

# --- Jira Entegrasyonu (Varsay?lanlar) ---
jira.default-email=${J?RA_EMA?L:}
jira.default-token=${J?RA_TOKEN:}
jira.default-domain=${J?RA_DOMA?N:}

# --- Email Configurations ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPR?NG_MA?L_USERNAME:seniorapp2test@gmail.com}
spring.mail.password=${SPR?NG_MA?L_PASSWORD:grisjtaqbmjwyfiy}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- File Upload & Storage ---
spring.servlet.multipart.max-file-size=-1
spring.servlet.multipart.max-request-size=-1
app.storage.base-path=./storage

# --- A? (OpenA?) configuration ---
openai.api-key=${OPENA?_AP?_KEY:}
openai.model=${OPENA?_MODEL:gpt-4o-mini}
ai.request-timeout-ms=${A?_REQUEST_T?MEOUT_MS:30000}

# CORS
app.cors.allowed-origins=http://localhost:5173