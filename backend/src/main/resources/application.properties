spring.application.name=senior-app
server.port=8080


# MySQL (override: MYSQL_PORT, MYSQL_USER, MYSQL_PASSWORD). Local default: root, no password, port 3333.
spring.datasource.url=jdbc:mysql://localhost:${MYSQL_PORT:3306}/seniorapp?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:BATU35kaan-?}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=CHANGE_ME_IN_PRODUCTION_use_a_long_random_secret_key_at_least_256_bits
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=http://localhost:5173

# Email Sending Configurations
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=seniorapp2test@gmail.com
spring.mail.password=grisjtaqbmjwyfiy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# GitHub OAuth — env overrides when set. Empty GITHUB_* becomes ""; AuthService uses github.internal.* as fallback.
github.client.id=${GITHUB_CLIENT_ID:Ov23li9glpCQOHx1keV0}
github.internal.default-client-id=Ov23lidzjwQNEQ4tC4D0
github.internal.default-client-secret=ffccbf38b88fff9e57af098108cea9df6b16d9f9
# Must match the "Authorization callback URL" in the GitHub OAuth app (Vite dev server).
github.redirect.uri=http://localhost:5173/auth/github/callback
github.client.secret=${GITHUB_CLIENT_SECRET:454a9c978382b3b8569532bdb3f4b687d40c1180}

# File Upload — no size limit
spring.servlet.multipart.max-file-size=-1
spring.servlet.multipart.max-request-size=-1

# Deliverable Storage
app.storage.base-path=./storage

